streamlit>=1.36
plotly>=5.18
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7
POT>=0.9

# Build script only (not needed for running the dashboard on cached CSVs)
moabb>=1.0
mne>=1.6

# DA4BCI powers DA Lab pages + build_moabb.py.
# For local dev you can also install from your clone:
#   pip install -e /path/to/DA4BCI-Python
da4bci @ git+https://github.com/Yiming-S/DA4BCI-Python.git

# CrossPython supplies feature / classifier code reused by the build script.
# If the build script isn't run in the deployment (only the dashboard reads the
# pre-built CSVs), this line can stay commented out.
# crosspython @ git+https://github.com/Yiming-S/CrossPython.git
