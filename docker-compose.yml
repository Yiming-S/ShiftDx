# Local dev convenience: `docker compose up` then open http://localhost:8501
services:
  shiftdx:
    build: .
    ports:
      - "8501:8501"
    volumes:
      # Mount data/ so freshly built CSVs appear without rebuilding the image.
      - ./data:/app/data:ro
    restart: unless-stopped
